{
  "name": "transport-erp",
  "version": "1.0.0",
  "description": "Transport Management System optimized for cPanel/GoDaddy",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "build": "npm install && npx prisma generate && cd client && npm install && npm run build",
    "dev:agent": "node scripts/dev-agent.js"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.0",
    "prisma": "^6.19.3"
  }
}
